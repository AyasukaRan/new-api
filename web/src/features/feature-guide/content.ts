/*
Copyright (C) 2023-2026 QuantumNous

This program is free software: you can redistribute it and/or modify
it under the terms of the GNU Affero General Public License as
published by the Free Software Foundation, either version 3 of the
License, or (at your option) any later version.

This program is distributed in the hope that it will be useful,
but WITHOUT ANY WARRANTY; without even the implied warranty of
MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE. See the
GNU Affero General Public License for more details.

You should have received a copy of the GNU Affero General Public License
along with this program. If not, see <https://www.gnu.org/licenses/>.

For commercial licensing, please contact support@quantumnous.com
*/
// Editorial content for the Chinese feature guide. Reader controls use i18n.
export type GuideArticle = {
  id: string
  title: string
  summary: string
  section: 'overview' | 'user' | 'admin'
  markdown: string
  keywords?: string[]
}

export const guideUpdatedAt = '2026-09-16'

export const guideArticles: GuideArticle[] = [
  {
    id: 'overview',
    title: '功能指南概述',
    summary: '从找到模型到完成调用，按角色查找本站的操作说明。',
    section: 'overview',
    keywords: ['入门', '开始', '角色', '权限'],
    markdown: `## 从这里开始

本站基于 new-api 提供统一的模型服务入口。普通使用者通过模型广场选模型、用平台 API 密钥连接应用，再从日志查看调用结果；管理员负责上游渠道、模型资料、定价、用户订阅与监控。

第一次使用，可以按下面的顺序操作：

1. 打开平台登录页，使用管理员开放的登录方式进入。
2. 打开「模型广场」，复制需要的模型名称，查看价格、支持的接口和当前可用状态。
3. 打开「API 密钥」，为自己的应用创建一把密钥，确认分组、有效期与额度限制。
4. 从「概览」或模型详情复制接口地址，填写到客户端，完成一次简短调用。
5. 到「使用日志」查看这次调用的结果、用量和计费来源。

## 按角色阅读

| 角色 | 主要阅读内容 | 操作范围 |
| --- | --- | --- |
| 使用者 | 登录、概览、模型广场、API 密钥、游乐场、使用日志、钱包与个人安全 | 自己的账户、密钥和记录，以及平台开放的公共内容 |
| 管理员 | 渠道、余额、测活、模型、用户与订阅、测试记录和请求追踪 | 管理权限允许的资源；敏感操作可能还需要单独授权 |
| 超级管理员 | 管理员指南中的模型定价、全局策略、任务插件与系统设置 | 全局配置及管理员权限管理 |

菜单由角色和站点配置共同决定。页面没有某个入口时，先确认权限和功能是否开放，不要仅凭文档中的入口名称判断账号异常。

## 本站使用时要特别分清

- **当前状态与历史可用率**：当前已恢复可用，不代表过去 24 小时的失败记录会立即消失。
- **钱包、订阅与密钥额度**：三者分别约束使用，密钥「无限配额」不代表账户无限可用。
- **使用日志与测试记录**：业务请求和游乐场调用属于使用日志；管理员渠道测试的用量单独查看。
- **全局与渠道定价**：同一模型可以有全局价格，也可以针对实际提供它的渠道设置覆盖。

本站的使用者和管理员指南均按已部署功能编写。金额、模型清单、时间与开关状态会变化，请以当前页面为准。

## 文档范围与项目来源

本文只说明控制台中的功能和操作，不包含安装部署、接口参数参考或插件开发教程。栏目组织参考 [New API 功能指南](https://docs.newapi.pro/zh/docs/guide/feature-guide)，具体操作与限制依据本站实现重新编写。new-api 及 QuantumNous 的项目来源和归属保持不变。`,
  },
  {
    id: 'sign-in',
    title: '登录与退出',
    summary: '登录平台并安全退出，区分网页会话与 API 密钥。',
    section: 'user',
    keywords: ['登录', '注册', '退出', 'token'],
    markdown: `## 登录平台

1. 打开平台登录页，选择当前提供的登录方式。
2. 使用账号密码、Passkey 或管理员配置的第三方账号完成验证。
3. 如果尚无账号，按页面提供的注册入口操作；未开放注册时联系管理员。

可用的登录与注册方式由管理员配置。通过第三方账号登录时，请在对应服务中处理该账号的密码问题。

## 正确退出

在账户菜单选择「登出」，并按提示确认。本站会撤销当前网页登录会话、清理本地登录状态并返回登录页。再次进入需要重新登录。

如果退出操作提示失败，不要把窗口消失当成已经退出；先根据页面反馈重试。共用设备上完成退出后再关闭页面。

## 网页登录与 API 密钥是两种凭据

退出网页登录针对当前网页会话，不会自动删除为客户端创建的 API 密钥。如果需要让某个脚本或客户端停止调用，应在「API 密钥」中单独停用、删除或更换对应密钥。

## 无法进入平台时

- 登录验证失败：根据页面反馈检查账号、密码、验证方式或网络状态。
- 账号被拒绝访问：联系管理员确认本站账号状态和授权。
- 已能登录却缺少某个菜单：确认角色及导航配置，不要反复创建新账号。`,
  },
  {
    id: 'dashboard',
    title: '概览与数据看板',
    summary: '查看账户额度、近期用量与模型调用趋势。',
    section: 'user',
    keywords: ['仪表盘', '统计', '额度', '消耗', '吞吐', '延迟'],
    markdown: `## 概览可以做什么

登录后进入「概览」，查看平台公告、API 接入信息、自己的用量摘要和服务状态。第一次接入时，从这里进入创建密钥、模型广场或游乐场。

有有效订阅时，额度摘要会优先展示订阅用量和可用情况，避免把钱包为零误解为账户完全不能调用。钱包余额仍是单独的计费来源；实际扣费还与订阅适用范围、可用额度和计费偏好有关。

## 查看一段时间内的使用情况

1. 打开「数据看板」。
2. 选择要查看的时间范围，并按页面提供的模型、分组等条件筛选。
3. 对比请求量、Tokens、消耗和性能趋势。
4. 要核对单次调用时，转到「使用日志」，使用相同的时间范围查找。

图表是汇总数据，不能代替单条日志中的计费明细。修改时间范围或统计口径后，数字不同是正常现象。

## 理解权限差异

普通用户查看自己可见的数据。管理员能看到的全站统计和用户分析由角色权限决定。不要用管理员全站图表直接对照个人钱包的变化。

## 读数提示

- 延迟表示等待时间，吞吐表示生成速度，两者含义不同。
- 订阅刷新时间是计划时间；管理员手动重置用量不会顺延这一时间。
- 当前可用状态可能已经恢复，而历史区间仍包含先前失败。`,
  },
  {
    id: 'model-square',
    title: '模型广场与可用性',
    summary: '查找模型、看清价格，并正确理解当前状态和历史色条。',
    section: 'user',
    keywords: ['成功率', '全红', '色条', '监控', '模型详情', '排行榜'],
    markdown: `## 找到适合的模型

1. 打开「模型广场」，搜索完整模型名称或使用厂商、分组、标签等筛选。
2. 查看简介、输入输出能力、价格和支持的接口。
3. 点击模型名称旁的复制按钮，取得客户端需要填写的模型标识。
4. 打开「详情」，查看规格、计费说明、接入信息和监控。

展示名称、厂商名和实际调用标识可能不同。客户端应使用复制到的模型标识，不要自行改写后缀。

## 看价格时同时看单位

输入、输出、缓存可以分别计价；另有按次或表达式计费的模型。模型广场展示本站对外提供的价格信息，不能当成上游厂商原价。不同分组、渠道和请求条件可能影响实际结算，最终以调用记录中的计费明细为准。

## 当前状态与最近 24 小时可用率

| 页面信息 | 用来判断什么 |
| --- | --- |
| 当前可用 / 当前不可用 | 在观测有效窗口内反映的服务状态 |
| 等待更新 / 未监控 | 观测已过期，或尚未有有效观测 |
| 最近 24 小时可用率 | 所选历史窗口内的总体表现 |
| 历史色条 | 不同时间段的成功、失败或无数据情况 |
| 延迟、吞吐 | 近期响应速度和生成速度 |

当前状态与后面的统计均结合真实请求和测活数据。模型由多个渠道提供时，会合并各渠道的可用情况；仍有可用提供渠道时，不会仅因为另一个渠道失败就判定模型整体不可用。进入详情可分别查看渠道表现。

分组用于访问与计费，不会把同一个渠道重复拆成多个可用性对象。当前观测的有效窗口跟随空闲测活间隔；过期时可能显示「等待更新」，没有观测时显示「未监控」。这两种情况都不应直接当成失败。

## 为什么调用正常，历史仍然偏红

恢复后的成功会更新当前状态，但过去的失败仍留在最近 24 小时统计中，直到逐渐离开统计窗口。先看当前状态和最近观测时间，再看历史比例，不要把「当前状态」与「整段历史全部成功」混为一谈。

## 管理员与普通用户看到的差异

普通用户看到汇总和匿名渠道状态。管理员可以进一步查看实际渠道名称、脱敏 Key、最近状态码和错误信息。这里的 Key 是脱敏标识，不是可复制调用的完整密钥。

排行榜用于观察使用热度与变化趋势，不代表某个模型一定适合当前任务，也不替代可用性检查。`,
  },
  {
    id: 'api-keys',
    title: 'API 密钥',
    summary: '为每个应用单独配置访问凭据和使用限制。',
    section: 'user',
    keywords: ['令牌', 'API Key', '有效期', '白名单', '分组'],
    markdown: `## 创建一把密钥

1. 进入「API 密钥」，点击创建入口。
2. 填写容易辨认的名称，例如「项目开发工具」。
3. 选择账号允许使用的分组。
4. 设置有效期、额度以及需要的模型限制和 IP 白名单。
5. 保存后，将密钥填入自己的客户端。

不同应用使用不同密钥，便于查看用量和单独停用。只在需要的时候查看或复制完整密钥，截图中应隐藏密钥内容。

## 各项限制分别控制什么

| 设置 | 作用 |
| --- | --- |
| 额度 / 无限配额 | 限制这把密钥可以消耗的额度；不增加账户余额或订阅额度 |
| 到期时间 | 超过时间后，该密钥不能继续正常使用 |
| 分组 | 在账号获准的范围内选择可访问的渠道与计费规则 |
| 模型限制 | 进一步收窄这把密钥可调用的模型 |
| IP 白名单 | 限定发起请求的来源地址 |

如果页面提供 Auto 分组顺序或跨分组重试，可按实际需求配置；它们不会让密钥获得账号本来没有的分组权限。

## 编辑、停用与删除

在密钥列表选择目标密钥，修改设置或停用。停用适合临时暂停客户端；不再使用时可以删除并在客户端移除旧凭据。更换密钥后，确认相关应用已同步更新。

## 模型存在却无法调用

依次检查密钥状态、到期时间、额度、分组和模型限制，再确认请求中的模型名称与模型广场一致。账户额度不足或当前没有可路由渠道，也可能导致请求被拒绝。`,
  },
  {
    id: 'clients',
    title: '客户端接入与聊天预设',
    summary: '从本站复制接入信息，避免填错地址、凭据或模型名称。',
    section: 'user',
    keywords: ['Base URL', '接入', '客户端', '聊天预设', '地址'],
    markdown: `## 客户端要填写的三个值

| 客户端字段 | 从哪里取得 |
| --- | --- |
| API 地址 / Base URL | 「概览」的 API 信息，或模型详情中的接入说明 |
| API Key | 本站「API 密钥」中创建的密钥 |
| Model | 模型广场复制的完整模型标识 |

不同客户端可能自动拼接接口路径。按本站的对应接入说明填写，尤其注意不要重复追加版本或请求路径。本站网页位于子路径下，复制地址时应保留页面给出的完整前缀。

## 完成一次最小验证

1. 先选择一种模型支持的接口格式。
2. 在客户端填写地址、密钥和模型名称。
3. 发送一段简短文本，确认能够收到回复。
4. 打开本站「使用日志」，检查对应请求的结果和用量。
5. 基础调用成功后，再按模型能力开启流式输出、工具调用或其他功能。

不要把上游渠道密钥填到本站客户端配置中；一般使用者只需要本站生成的 API 密钥。

## 使用聊天预设

如果平台开放「聊天预设」或客户端导入入口，可选择管理员提供的预设打开目标应用。导入前确认目标客户端和所用密钥，导入后核对地址及模型配置。

预设只是简化配置，不会绕过模型、分组或额度限制。没有适合的预设时，可按上表手动填写。`,
  },
  {
    id: 'playground',
    title: '游乐场',
    summary: '在网页中验证模型回复，再按日志核对消耗。',
    section: 'user',
    keywords: ['在线聊天', '测试', '附件', '联网搜索', '对话'],
    markdown: `## 发起一轮对话

1. 打开「游乐场」。
2. 选择可用分组和模型，确认账户额度满足调用条件。
3. 填写消息，按需设置页面开放的参数。
4. 发送后查看回复；需要中止生成时使用停止操作。
5. 到「使用日志」确认用量、耗时和计费。

游乐场发起的是正常模型请求，通常会按本站规则计费。它不会因为是在网页中体验，就自动变成管理员的免费渠道测活。

## 使用对话上下文

后续消息可能携带当前会话中已有的内容，影响输入 Tokens 和费用。需要独立验证时，使用「清空聊天记录」后再发送简短问题。当前聊天记录仅保存在当前浏览器，不会自动跨设备同步。

模型能力由上游接口和渠道配置决定。切换模型后，要同时核对该模型支持的参数和能力，不要仅沿用上一个模型的设置。

## 当前限制

当前页面中的附件与联网搜索入口尚未提供完整可用能力，不能据此认为已支持上传文件或联网检索。需要这些能力时，先查看模型详情及本站已经开放的客户端接入方式。

一次成功对话只能证明这次请求成功，不会清除此前的失败历史。测试不同模型时，结合当前状态和最近调用结果判断。`,
  },
  {
    id: 'usage-logs',
    title: '使用日志与异步任务',
    summary: '定位自己的请求，查看费用、用量、耗时和失败原因。',
    section: 'user',
    keywords: ['日志', '错误', '失败', 'Tokens', '费用', '异步任务'],
    markdown: `## 查找一次调用

1. 进入「使用日志」，将时间范围缩小到调用前后。
2. 按模型、密钥、日志类型等页面提供的条件筛选。
3. 打开对应记录的详情。
4. 核对时间、模型、输入输出 Tokens、费用、计费来源和错误信息。

普通用户只查看自己的可见记录。管理员可以在获准范围内查看更多用户和渠道信息。

## 常见字段

- **输入 / 输出 Tokens**：本次请求计量的文本等用量；缓存项目可能另行展示。
- **费用及计费来源**：结合钱包、订阅或动态计费详情理解实际结算。
- **首字时间**：开始收到输出前的等待时间；并非所有请求都有这一读数。
- **总耗时与吞吐**：用于排查慢请求，不能仅用一个指标判断模型质量。
- **错误信息 / 请求标识**：提供给管理员定位问题时的主要线索。

失败记录不一定有完整的用量和费用。不要把缺失读数直接理解为零成本或零耗时。

## 管理员测试不在这里混排

渠道模型测试产生的可识别用量进入管理员的「测试记录」。游乐场和业务客户端发起的普通请求仍然属于使用日志，即使你的目的只是测试一下模型。

## 异步任务

如果平台开放绘图或异步任务记录，可以在相应页面查看任务状态、结果和错误。提交成功只代表任务被接收，不等于图片、视频或其他结果已经生成完成；以最终任务状态为准。

## 请求追踪的访问范围

完整请求追踪目前仅对管理员开放。普通用户排障时，提供发生时间、模型、错误信息和请求标识，由管理员在授权范围内进一步查看。`,
  },
  {
    id: 'wallet',
    title: '钱包与订阅',
    summary: '分清账户余额、订阅周期和密钥限额，理解刷新规则。',
    section: 'user',
    keywords: ['余额', '订阅', '刷新', '重置', '计费偏好', '充值'],
    markdown: `## 三种额度不要混在一起

| 项目 | 含义 |
| --- | --- |
| 钱包余额 | 账户钱包中剩余的可用额度 |
| 订阅额度 | 某一套餐在当前周期或总有效期内可使用的额度 |
| API 密钥额度 | 对某一把密钥额外施加的使用限制 |

钱包余额为零时，适用且有剩余额度的订阅仍可能继续承担调用。订阅存在也不代表任何模型、分组或密钥都能无限使用。

## 查看订阅状态

1. 打开「钱包」，查看已有订阅。
2. 核对套餐名称、状态、到期时间和剩余天数。
3. 查看当前已用 / 总额度，以及下一次刷新时间。
4. 如页面开放计费偏好，选择适合自己的钱包或订阅使用顺序。

实际能否使用订阅，还取决于有效期、剩余额度、模型或分组限制以及当前计费规则。「优先订阅」不等于订阅不足时一定能扣钱包；已有有效订阅但额度不足时，还要看套餐是否允许钱包溢出。概览优先展示订阅，是展示方式，不会替代这些规则。

## 周期刷新与手动重置

有周期的套餐按照计划刷新用量。管理员为你手动重置用量时，原定的下一次刷新时间不变。

例如原定 20:00 刷新，15:00 管理员执行重置后，当前用量重新计算，下一次计划刷新仍是 20:00。没有自动刷新周期的套餐，以页面显示的额度和有效期为准。

## 充值、兑换和订单

钱包只展示平台当前开放的充值、兑换或套餐购买方式。按页面显示的金额和方案操作，完成后核对余额、订阅状态与订单记录。遇到支付或到账问题，保留订单号并联系管理员，不要反复提交同一笔操作。`,
  },
  {
    id: 'account-security',
    title: '个人资料与账户安全',
    summary: '管理个人偏好，检查登录会话与已开放的安全功能。',
    section: 'user',
    keywords: ['个人资料', '安全', 'Passkey', '两步验证', '登录会话'],
    markdown: `## 个人资料

进入「个人资料」，维护允许修改的信息、通知方式和界面偏好。修改本站显示信息不会更改已关联的第三方账号资料。

语言、主题或侧边栏显示等偏好，只调整界面体验，不会扩大账号权限。被管理员关闭的模块不会因为修改个人偏好而重新开放。

## 账户安全

在「安全与访问」查看平台开放的安全能力，例如两步验证、Passkey 和登录会话。只有页面当前提供的功能才能配置，允许使用的登录方式由管理员决定。

涉及绑定、解除绑定、查看敏感信息或安全设置时，按页面要求完成身份验证。不要把验证码、恢复码或完整密钥发给他人。

## 检查登录会话

查看当前设备和其他设备的会话。发现不再使用或不认识的会话时，可按页面提供的操作撤销；撤销当前会话会使当前网页退出登录。

如果怀疑某个客户端凭据泄露，除了检查网页登录会话，还应到「API 密钥」处理对应密钥。这两类凭据的生命周期不同。`,
  },
  {
    id: 'channels',
    title: '渠道配置与多 Key',
    summary: '维护上游服务及其模型清单，区分配置状态和运行状态。',
    section: 'admin',
    keywords: ['渠道', '多密钥', 'Key', '模型映射', '优先级', '权重'],
    markdown: `## 创建或编辑渠道

入口为「渠道」。管理员在权限允许的范围内创建、编辑和操作渠道；查看完整密钥、修改敏感字段等操作可能需要单独授权。

1. 选择与上游相符的渠道类型，填写容易辨认的名称。
2. 按该类型要求填写服务地址与密钥。
3. 配置此渠道实际提供的模型清单及分组。
4. 按需要配置模型映射、优先级、权重或其他已开放的路由设置。
5. 保存后测试目标模型，并到监控查看结果。

模型名称和映射必须与上游实际能力对应。修改模型资料不会自动修改渠道的模型列表。

## 多 Key 渠道

当多个账户提供同一类服务时，可以按渠道表单要求配置多个 Key，并选择页面支持的分配方式。管理界面会区分各个 Key 的索引、状态和脱敏标识。

每个 Key 都可能具有独立余额和故障状态。一个 Key 可用，不代表其余 Key 正常；同样，一个 Key 失败也不一定代表整个渠道不可用。

## 区分三种信息

| 信息 | 说明 |
| --- | --- |
| 渠道启停配置 | 管理员决定该渠道是否参与服务 |
| 运行可用状态 | 最近真实调用与测活反映的状态 |
| 余额状态 | 最近成功查询到的账户余额及明细 |

测活用于监控，不会按测试结果自动修改渠道启停配置。已确认的负余额会影响真实转发选择，但不会把渠道开关改成停用。

## 分组与可用性

分组控制访问范围及相关计费配置。一个渠道属于多个分组时，监控仍按渠道与模型汇总，不能把重复的分组记录当成不同渠道。`,
  },
  {
    id: 'channel-balance',
    title: '渠道余额与用量监控',
    summary: '查看各 Key 余额，配置自动刷新，并理解负余额过滤。',
    section: 'admin',
    keywords: ['负余额', '总余额', '自动刷新', '余额开关', '用量监控'],
    markdown: `## 查看汇总与明细

在「渠道」列表查看余额。多 Key 渠道默认展示可用余额汇总，鼠标停在余额上可查看各个账户明细；余额与用量监控中可以查看变化和更新时间。

多 Key 汇总只累加已知的正余额，负余额保留在明细中，不抵扣其他账户的可用资金。例如三把 Key 分别为 -20、100、30，汇总可用余额是 130。

某些供应商不支持余额查询，或者查询返回不完整。遇到错误时要看查询状态和更新时间，不能把上次完整结果当成刚刚全部查询成功。

## 控制单个渠道是否查询余额

1. 在「渠道 → 编辑 → 其他设置」找到「余额查询」。
2. 关闭并保存后，该渠道不参与手动、批量和定时余额查询。
3. 需要恢复时重新打开，再执行刷新。

关闭查询会保留既有记录，不会把已知负余额直接改成未知或恢复正常。

## 开启定时余额刷新

超级管理员进入「系统设置 → 模型 → 路由可靠性」，在「余额监控」中打开「自动刷新余额」，设置「余额刷新间隔（分钟）」并保存。

全局自动刷新只处理允许查询余额的渠道。全局开关、刷新间隔与单渠道开关共同决定一个渠道是否会被定时查询。

## 负余额怎样影响真实请求

- 单 Key 渠道已知余额为负时，正常转发会跳过该渠道。
- 多 Key 渠道只排除负余额的 Key，其他可用 Key 仍能参与。
- 所有可用 Key 都因负余额不可选时，该渠道不能承担这次正常转发。
- 充值后刷新余额，确认非负读数后才能恢复相应的余额判定。

余额未知、查询报错或恰好为零，不会单凭这一点当成「已知负余额」。下一次查询失败，也不会自动解除之前已确认的负余额。

负余额过滤针对真实转发。管理员查询和模型测活仍可运行，因此不要把「不接真实请求」理解成「永远不再检测」。

## 渠道监控与统计范围

在渠道行菜单选择「渠道监控」，可切换最近 24 小时或最近 7 天，并查看余额历史及分 Key 明细。请求尝试次数包含重试；费用和用量来自真实计费记录，不含测活用量；可用率则包含真实请求与测活。这些指标的统计范围不同。

平台用量按本站计费规则统计，上游余额来自供应商账户，二者不必逐笔相等。查看趋势时同时核对币种、时间范围、查询成功状态和具体 Key。`,
  },
  {
    id: 'health-checks',
    title: '测活与可用性监控',
    summary: '按渠道、模型和 Key 观察服务，理解空闲测活的计时规则。',
    section: 'admin',
    keywords: ['测活', '定期渠道测试', '空闲', '计时', '可用率', '重置计时'],
    markdown: `## 开启定时测活

超级管理员进入「系统设置 → 模型 → 路由可靠性」，在「渠道健康检查」中启用「定期渠道测试」，设置「空闲测活间隔（分钟）」和合适的测试并发数，然后保存。

自动测活会检查参与测试渠道下配置的模型，不是只测一个默认模型。多 Key 渠道会分别观察启用的 Key；同一渠道的某个模型只要有可用 Key，不能仅因另一把 Key 失败就判断整个渠道模型不可用。

## 真实请求会重置空闲计时

计时对象是「渠道 + 模型」。这个组合发生真实调用，无论成功还是失败，都会重新开始空闲计时；其他渠道或其他模型的计时不受影响。

例如间隔为 20 分钟，模型 A 在渠道甲 10:30 被调用，那么这个组合需要从 10:30 开始完整空闲 20 分钟后才满足自动测活条件。若 10:45 又发生调用，则继续从 10:45 重新计时。

所以持续有请求的模型可能很少出现自动探测，这是预期行为。调度执行有扫描周期，不应把间隔当成精确到秒的整点计划。手动测试可以主动发起，不受同样的空闲等待限制。

## 测试结果如何展示

- 渠道状态、模型详情和历史统计共同使用真实请求与测活观测。
- 模型整体状态合并各个提供渠道；仍有可用渠道时可以整体可用。
- 管理员可展开实际渠道和脱敏 Key，查看最近观测、状态码与错误。
- 同一渠道的所有启用 Key 都有新鲜失败观测时，才判为当前不可用；未知或过期不能当成失败。
- 观测的有效窗口随空闲测活间隔变化，超过窗口后等待新的请求或测试更新。
- 普通用户只看到汇总或匿名渠道展示，不显示真实渠道名和错误明细。

## 测活不会替你切换渠道开关

当前测活只用于监控，不会因为一次失败自动关闭渠道，也不会因为一次成功自动重新开启手动关闭的渠道。定时测试不处理手动禁用的渠道。

测试请求用于验证模型接口是否能正常响应，不代表业务问题的答案质量。如果出现输入格式、模型映射或供应商策略错误，应结合具体 Key 的错误处理，而不是只看渠道有无余额。

## 恢复后怎样确认

修正配置或充值后，刷新余额并执行需要的测试，查看最近观测是否更新。当前状态可以先恢复，过去 24 小时的失败仍会保留在历史统计中。`,
  },
  {
    id: 'model-management',
    title: '模型资料与渠道覆盖',
    summary: '维护对外展示的模型信息，并按实际渠道清单判断模型是否仍存在。',
    section: 'admin',
    keywords: ['模型资料', '渠道覆盖', '标签', '模型清理', '下线'],
    markdown: `## 编辑模型资料

进入「模型」，找到目标模型，打开编辑面板。根据页面分区维护模型名称、简介、供应商、图标、标签、匹配规则与端点配置。

只填写已经核实的能力，不要仅凭模型名称推断图片、音频或工具调用支持。资料更新后到模型广场确认用户看到的效果。

## 核对渠道与分组

模型面板会展示提供该模型的渠道及关联分组。这里适合回答「谁提供这个模型」；实际接入能力仍由对应渠道的模型列表、映射和路由配置决定。

修改模型资料中的名称，不会自动改掉上游渠道模型列表，也不会自动把原名称的价格搬到新名称。重命名或别名调整后，要一并检查调用标识和价格。

## 资料与价格分别保存

模型资料、定价和渠道相关配置各有保存范围。保存资料不代表保存价格；离开编辑面板前留意未保存提示。

## 清理已不再提供的模型

本站采用的维护口径是：只有所有现有渠道都不再提供某个模型时，才将其纳入清理。仍有任一渠道提供就保留。

这是维护判断规则，不是对所有模型自动定时删除的承诺。执行清理前先核对渠道模型列表与映射，不要把临时失败、余额不足或某一渠道下线当成模型永久消失。`,
  },
  {
    id: 'model-pricing',
    title: '全局与渠道模型定价',
    summary: '在同一编辑器选择定价范围，保留明确的覆盖和继承关系。',
    section: 'admin',
    keywords: ['价格', '全局', '渠道定价', '继承', '表达式', '币种'],
    markdown: `## 打开定价编辑器

模型定价编辑需要超级管理员权限。进入「模型」，打开目标模型的「定价」；也可从「系统设置 → 计费 → 模型定价」进入。

1. 确认正在编辑的模型。
2. 在「定价范围」选择「全局」或实际提供该模型的渠道。
3. 选择按量、按次或表达式等适用的计费方式。
4. 核对币种、用量单位、输入输出和缓存项目。
5. 保存当前范围的配置。

全局与渠道使用同一套价格编辑界面。渠道选项主要列出提供该模型的渠道；已有历史价格覆盖的渠道也可能保留，并标为「不可用渠道」，便于清理旧配置。

## 覆盖和继承

| 当前选择 | 配置效果 |
| --- | --- |
| 全局 | 设置模型的全局价格 |
| 指定渠道 | 只对该渠道上的此模型建立覆盖 |
| 渠道没有覆盖 | 继承适用的全局价格 |
| 使用全局价格 | 移除渠道覆盖，重新跟随全局配置 |

填写 0 与留空不一定相同：0 可以表示明确的免费价格，留空可能表示未配置或继承。请结合项目旁的说明确认，不要用留空替代明确零价。

## 调整价格前先看预览

价格预览可以帮助理解缓存、图片、音频等项目及示例用量的费用。示例不是实际扣费记录，最终费用仍取决于请求用量和适用规则。

切换显示币种是编辑和阅读辅助。切换前后应核对单位与换算后的数值，未保存修改要按页面提示处理。

## 自动转换为表达式

全局旧定价可以在支持的情况下预览转换为表达式，确认后再应用。渠道继承与全局配置的语义不同，界面不会把全局转换能力直接当成渠道转换。

如果缓存与图片或音频的重叠用量无法保持原计费结果，页面会提示需要手动转换，并保留原配置。此时不要以「看起来差不多」的表达式直接替换生产价格，应先核对相应用量情形。

任务插件的变体和用量单位依赖插件定义，不应当作普通文本模型的输入输出倍率填写。`,
  },
  {
    id: 'users',
    title: '用户与权限管理',
    summary: '查看用户状态和额度，在授权范围内调整账户。',
    section: 'admin',
    keywords: ['用户', '权限', '角色', '分组', '额度', '用户ID'],
    markdown: `## 找到用户

进入「用户」，按用户名等条件定位账户，核对身份、角色、分组和状态。使用额度列查看钱包余额、订阅已用 / 总额度及下一次刷新时间。

有多个订阅时，摘要不能代替完整列表；使用「管理订阅」查看各项套餐的状态与有效期。

## 管理账户

在权限允许的范围内编辑用户信息、调整分组或额度，并按需要处理账户启停。角色权限与普通资料不同，应核对目标用户和操作范围后保存。

管理员自身也可能被设置操作权限覆盖，因此「是管理员」不等于能查看完整渠道密钥或修改所有全局设置。模型定价、任务插件和系统设置等全局能力由超级管理员管理。

## 调整钱包与重置订阅是两件事

调整用户钱包额度不会自动清空订阅用量。要恢复某个订阅周期的可用额度，使用对应的订阅管理入口，不要通过修改钱包余额替代。

用户 ID 是记录关联标识，可能因为历史数据或删除操作而不连续。排查时用具体账号和记录标识定位，不应仅按行号猜测同一用户。`,
  },
  {
    id: 'subscriptions-admin',
    title: '订阅套餐与用量重置',
    summary: '维护套餐，为指定用户重置用量，同时保持原刷新计划。',
    section: 'admin',
    keywords: ['套餐', '管理订阅', '重置用量', '刷新时间', '周期'],
    markdown: `## 管理套餐

进入「订阅」查看和维护套餐。创建或编辑时，核对额度、有效期、刷新周期及页面提供的使用限制；套餐展示和可购买状态也以当前配置为准。

编辑前分清是在修改套餐规则，还是在处理某个用户已经持有的订阅。不同操作的影响范围可能不同，按页面确认信息执行。

## 查看用户的订阅使用情况

1. 在「用户」列表定位账户。
2. 查看额度列中的订阅摘要和下一次刷新时间。
3. 打开「管理订阅」，核对套餐、状态、有效期和用量。

## 重置指定用户的用量

1. 在用户的订阅列表找到要处理的有效订阅。
2. 点击「重置用量」。
3. 核对确认框中的用户、套餐和影响范围后执行。
4. 返回列表检查用量已经重置，下一次刷新时间保持原计划。

这项操作会一起清理该用户在所选套餐下所有有效订阅的已用额度，而不是只处理点击的单条记录。原定重置时间保持不变。例如原定 20:00 刷新，15:00 手动重置后，下一次仍是 20:00。

## 套餐批量重置

订阅管理中也提供按套餐处理有效订阅的入口。它影响的用户范围更大；只有确实需要对整个套餐统一处理时才使用。不要把批量重置当成处理单个用户的快捷方式。

重置用量不是修改历史账单，也不会抹掉先前的使用记录。`,
  },
  {
    id: 'test-records',
    title: '测试记录与日志管理',
    summary: '把业务调用与渠道测试分开查看，正确解释测试费用。',
    section: 'admin',
    keywords: ['测试记录', '使用记录', '测活费用', '估算', '日志管理'],
    markdown: `## 两个列表分别看什么

| 入口 | 主要内容 |
| --- | --- |
| 使用日志 | 正常业务、客户端及游乐场请求产生的记录 |
| 测试记录 | 可识别的渠道模型测试用量，包含历史测试记录 |

「测试记录」是管理员入口。测试记录费用仅为估算，不扣除用户额度；正常游乐场调用仍按普通请求计费。

## 查看测试记录

1. 打开「测试记录」。
2. 按时间、模型、渠道等页面提供的条件筛选。
3. 核对测试产生的 Tokens、估算费用与耗时。
4. 要看失败或某个 Key 的状态时，转到模型或渠道监控。

测试列表主要展示成功且产生用量的数据。失败测活不能仅靠此列表查全，因此不要以测试记录行数作为全部测活次数来计算成功率。

## 排查真实调用

在「使用日志」选择适当的查看范围，先缩小时间，再筛选用户、模型、渠道或错误类型。判断是否影响用户时，要查看实际请求结果和重试情况，而不是只看某个上游尝试失败。

错误摘要不一定包含完整请求过程；需要授权范围内的深入分析时再打开请求追踪。`,
  },
  {
    id: 'request-tracing',
    title: '请求追踪与聚合对话',
    summary: '管理员按请求查看对话内容、上游交互和重试。',
    section: 'admin',
    keywords: ['追踪', '对话聚合', '交互详情', '重试', '过期'],
    markdown: `## 打开请求追踪

请求追踪目前仅管理员可见。在「使用日志」中定位记录，使用页面提供的追踪入口。能否展示完整内容取决于是否曾采集、是否仍在保留期内以及请求是否关联成功。

查看业务对话和上游交互前，确认自己有相应排障职责。分享截图时隐藏个人信息、业务内容及凭据。

## 对话视图

「对话」将这一次请求携带的历史消息与最终响应整理成便于阅读的内容，包括支持展示的用户消息、模型回复与工具调用。

这里的聚合并不等于自动把所有独立请求合成一条跨请求会话。客户端没有携带、平台没有采集或已过期的内容，不会在这个视图中凭空出现。

## 交互详情

「交互详情」用于查看请求交换、渠道选择后的实际转发与重试等过程。排查时可依次核对：

1. 客户端请求的模型和消息内容是否符合预期。
2. 实际上游模型是否受到映射或参数覆盖影响。
3. 某次失败后是否重试，最终响应来自哪次交互。
4. 错误发生在接口校验、上游响应还是传输过程。

## 显示未记录或已过期

这表示当前没有可用的追踪内容，不代表该条使用日志本身无效，也不证明请求从未发生。继续使用日志中的时间、请求标识、错误和计费信息排查，必要时检查采集与保留配置后复现。

超级管理员可在「系统设置 → 运维 → 日志维护」设置「记录完整请求追踪」及保留期限。追踪与用量日志分别保存、分别过期。开启采集只影响后续符合条件的请求，不能补回过去没有记录的正文。`,
  },
  {
    id: 'groups',
    title: '分组与访问范围',
    summary: '让用户、密钥和渠道的访问范围保持一致。',
    section: 'admin',
    keywords: ['分组', '倍率', 'Auto', '跨分组重试', '访问范围'],
    markdown: `## 分组参与哪些配置

用户分组、密钥所选分组和渠道所属分组共同影响请求的可用范围，分组也可用于差异化计费。分组不是独立的上游账户，也不是监控中新增的一条渠道。

## 新增或调整分组时

1. 在相应分组和计费设置中确认该分组已登记并配置。
2. 确认哪些用户可以使用它。
3. 为实际提供模型的渠道配置对应分组。
4. 检查用户密钥是否选择了允许的分组。
5. 用该分组完成一次调用，再查实际路由和费用。

只在某一处填写一个分组名称，并不保证从用户到渠道的整条访问路径已经配置完整。

## 自动分组与重试

如果使用 Auto 分组，核对可选范围、顺序和跨分组重试设置。重试只应在获准的分组范围内进行，不会自动扩大账号权限。

同一渠道被多个分组引用时，模型可用性仍按实际渠道合并展示；不要通过复制分组试图修正可用率。`,
  },
  {
    id: 'system-settings',
    title: '系统设置与任务插件',
    summary: '找到日常管理所需的设置域，按实际权限和插件能力配置。',
    section: 'admin',
    keywords: ['系统设置', '任务插件', '公告', '文档入口', '全局开关'],
    markdown: `## 系统设置入口

系统设置由超级管理员维护，按站点、身份验证、计费、模型、安全、控制台内容和运维等领域组织。需要修改某项行为时，先找对应领域，避免在多个地方重复或相互覆盖地配置。

| 需求 | 对应设置方向 |
| --- | --- |
| 站点名称、导航和文档入口 | 站点与品牌相关设置 |
| 登录方式、OAuth 与 Passkey | 身份验证 |
| 钱包、计费与支付 | 计费相关设置 |
| 模型定价 | 计费 → 模型定价 |
| 路由、定期测试与余额刷新 | 模型 → 路由可靠性 |
| 访问和请求限制 | 安全与限制 |
| 概览中的 API 信息、公告和常见问题 | 控制台内容 |
| 运行与监控相关维护 | 运维及系统信息 |

具体菜单中文名称可能随界面版本调整。测活和余额定时开关位于「模型 → 路由可靠性」，不要只在单个渠道里寻找全局调度开关。

## 修改配置后核对结果

保存后查看成功反馈，再回到相关功能页验证。对访问限制、订阅与计费的修改，应核对实际受影响的对象和范围。

本站只记录使用操作，不提供这里的部署参数、数据库凭据或服务器维护步骤。

## 任务插件

「任务插件」由超级管理员管理。使用前确认插件描述、所需配置、支持的任务类型和用量单位。安装或启用插件不等于上游账户已经具备调用权限；仍需完成相应渠道配置。

任务插件可能使用秒数、次数或其他用量进行计费。配置模型价格时以插件声明的用量字段为准，不要直接套用普通文本模型的每百万 Tokens 单价。

插件版本与启用状态变化后，先验证一个符合插件能力的任务，再检查任务结果和计费记录。`,
  },
  {
    id: 'troubleshooting',
    title: '常见问题与排查顺序',
    summary: '从错误现象出发，找到需要检查的账户、渠道或监控信息。',
    section: 'overview',
    keywords: ['帮助', '故障', '全红', '没有监控', '余额不足', '追踪为空'],
    markdown: `## 模型调用正常，为什么历史色条仍红

当前状态与最近 24 小时历史是两个指标。新的成功观测可以恢复当前状态，但历史失败不会立即清空。查看最近观测时间；管理员进一步检查具体渠道和 Key。

## Key 都有余额，为什么仍然失败

余额只说明查询时账户的资金情况，不保证密钥权限、模型名称、请求格式、额度策略或供应商服务正常。先刷新余额，再按最近错误逐项排查。没有新请求或测活观测时，监控状态不会仅因余额变化就代表请求已经成功。

## 一个渠道失败，为什么模型还显示可用

模型可以由其他渠道继续提供。详情页分别列出渠道情况；模型整体合并后仍可能可用。

## 钱包是零，是不是不能调用

检查是否有有效、适用且有剩余额度的订阅，同时核对密钥限制与计费偏好。概览优先展示订阅情况，不代表所有请求都能使用该套餐。

## 为什么看不到某个模型的测活

管理员检查渠道模型清单、映射、是否参与测试，以及该渠道和模型是否持续有真实请求。真实请求会重置空闲计时。普通用户只看到自己可见范围内的模型与状态。

## 为什么负余额 Key 仍出现检测记录

负余额过滤作用于正常转发；管理员查询和测活可以继续运行。区分业务请求与检测记录，再检查观察时间，避免把旧失败当成刚发生的请求。

## 为什么使用日志没有渠道测试

渠道测试的可识别用量在管理员「测试记录」。失败测活去渠道或模型监控查看。客户端和游乐场的正常调用仍在「使用日志」。

## 为什么重置用量后仍在原时间刷新

手动重置只清理用量，不改变下一次计划刷新时间。

## 请求追踪为什么打不开

完整追踪需要管理员权限，且内容曾被采集并仍在保留期内。记录未采集、已过期或关联缺失时，可继续依据使用日志排查。

## 联系管理员时提供什么

提供发生时间、模型标识、请求标识和错误摘要，并说明是普通调用、手动测试还是自动测活。不要提供完整密钥、验证码、恢复码或不必要的业务对话正文。`,
  },
]
